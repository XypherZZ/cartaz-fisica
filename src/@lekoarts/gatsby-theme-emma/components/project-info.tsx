/** @jsx jsx */

type ProjectInfoProps = {
  project: {
    client: string
    date: string
    service: string
  }
}

const ProjectInfo = ({ project }: ProjectInfoProps) => null

export default ProjectInfo
